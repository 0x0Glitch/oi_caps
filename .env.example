# HIP-3 OI Cap Manager Configuration

# ==================== REQUIRED ====================

# Deployer private key (must be the HIP-3 DEX deployer)
# Example: 0x1234567890abcdef...
HIP3_DEPLOYER_PRIVATE_KEY=

# DEX identifier (the name field from perpDexs response)
# Example: felix, sports, btcx, etc.
HIP3_DEX_NAME=

# Asset/market name to update (as shown in meta endpoint)
# Example: BTC, ETH, felix:TSLA, sports:JALLEN17
HIP3_MARKET_NAME=

# New OI cap in USD (must be whole number)
# Constraints: >= max($1,000,000, 0.5 * current_OI)
# Examples: 5000000 (5M), 10_000_000 (10M), 1e7 (10M)
HIP3_NEW_CAP=

# ==================== OPTIONAL ====================

# Network selection (optional, defaults to false for testnet)
# Set to true for mainnet, false for testnet
HIP3_IS_MAINNET=false

# Dry run mode (optional, defaults to true for safety)
# Set to false to execute real transactions
HIP3_DRY_RUN=true

# Maximum allowed cap change percentage (optional, default: 200%)
# This is a safety feature to prevent accidental large cap changes
# E.g., 200 means max 200% change allowed (3x increase or 67% decrease)
HIP3_MAX_CAP_CHANGE_PERCENT=5

# Enforce cap >= current OI (optional, default: false)
# If true, prevents setting caps below current open interest
# This is STRICTER than the official HIP-3 spec, which only requires max($1M, 0.5*OI)
HIP3_ENFORCE_CAP_ABOVE_OI=false
